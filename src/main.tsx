import React from 'react'
import ReactDOM from 'react-dom/client'
import LayerBoard from './LayerBoard.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LayerBoard />
  </React.StrictMode>
)
